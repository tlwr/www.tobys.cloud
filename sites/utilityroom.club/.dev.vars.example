# Copy to .dev.vars for local wrangler dev (not committed).
# Must match sites/auth.tobys.cloud/.dev.vars AUTH_JWT_SECRET.
AUTH_JWT_SECRET=replace-me-with-the-same-value-as-auth
AUTH_ISSUER=http://localhost:8788
