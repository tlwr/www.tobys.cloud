---
apiVersion: v1
kind: Service
metadata:
  name: www-tobys-cloud
  namespace: sites
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: www-tobys-cloud
...

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: www-tobys-cloud
  namespace: sites
spec:
  selector:
    matchLabels:
      app: www-tobys-cloud
  replicas: 1
  revisionHistoryLimit: 3
  template:
    metadata:
      labels:
        app: www-tobys-cloud
    spec:
      containers:
      - name: nginx
        image: ghcr.io/tlwr/www.tobys.cloud:24b12c521c5490c98fe1ea73564c04c436fa9086
        ports:
        - containerPort: 80
...

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: www-tobys-cloud
  namespace: sites
  annotations:
    kubernetes.io/tls-acme: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: haproxy
  rules:
  - host: tobys.cloud
    http:
      paths:
      - backend:
          service:
            name: www-tobys-cloud
            port:
              number: 80
        path: /
        pathType: Prefix
  - host: www.tobys.cloud
    http:
      paths:
      - backend:
          service:
            name: www-tobys-cloud
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - secretName: www-tobys-cloud-tls
    hosts:
      - www.tobys.cloud
      - tobys.cloud
